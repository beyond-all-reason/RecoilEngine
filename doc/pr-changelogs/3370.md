 * added the `isFactory` unit def tag (`Engine.FeatureSupport.isFactoryTag`). Defaults to what the engine infers today, a builder that is immobile and has a `yardMap`; setting it false makes such a def a regular static builder that keeps its assist commands. Setting it true on a def that does not qualify is warned about and ignored.
