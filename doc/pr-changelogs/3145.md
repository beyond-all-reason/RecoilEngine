* `unbind` now compares the action name and its arguments separately, so `unbind a firestate 2` removes just that variant, and `unbind a firestate` removes nothing if only `firestate 2` is bound. `unbindaction` still sweeps every variant when given a bare name, but narrows to one when arguments are named.
