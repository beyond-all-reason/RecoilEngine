Caveats:
* `CMD_INSERT` passes its command timeout to the inserted command. Added `Engine.FeatureSupport.commandInsertPassesTimeout` boolean, `true` on engines that support this.

Misc:
* add `Spring.SetUnitCommandTimeout` to synced lua handles to apply and modify timeouts on queued commands.
