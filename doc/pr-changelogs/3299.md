 * add `gl.CopyImageSubData(srcName, srcLevel, srcX, srcY, srcZ, dstName, dstLevel, dstX, dstY, dstZ, width, height, depth)`, a Lua binding of glCopyImageSubData (registered when ARB_copy_image is available). Copies texture data directly between textures, compressed formats and texture-array layers included, without a render-to-texture pass.
