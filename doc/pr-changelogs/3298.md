 * DDS textures with the DX10 extended header now load, and BC4, BC5 and BC7 compressed DDS files are supported (mapped onto RGTC and BPTC). BC7 blocks cannot be vertically flipped on load, so BC7 files should be authored pre-flipped (a one-time warning is logged).
