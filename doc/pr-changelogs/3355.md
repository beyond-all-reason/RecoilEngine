* added the synced `Spring.ClearUnitAttackers(unitID)` callout (requires full control); it removes unit-targeted attack, fight and manual-fire orders against the unit and clears weapon targets on it without changing its neutral state
