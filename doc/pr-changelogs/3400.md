* declare per-monitor DPI awareness on Windows. Stops DWM bitmap-stretching the window on monitors whose scale factor differs from the primary one, notably the DualScreenMode secondary screen on mixed-DPI setups. A windowed session on a scaled monitor now renders at its true pixel size: sharper, physically smaller. Fullscreen is unaffected.
