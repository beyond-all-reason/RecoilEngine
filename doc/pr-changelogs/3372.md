 * metal extraction is now a component a unit owns rather than a unit type. Any immobile def that sets `extractsMetal` extracts, whatever class it resolves to, so an extractor no longer has to be a building. `extractsMetal` on a mobile def is warned about and ignored.
