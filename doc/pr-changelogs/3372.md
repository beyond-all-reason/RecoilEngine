 * metal extraction is now a component a unit owns rather than a unit type, so an extractor no longer has to be a building. An immobile def without a `yardMap` can extract, and a `builder` can extract. `extractsMetal` on a mobile def is warned about and ignored.
